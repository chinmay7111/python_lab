str = 'hello chinmay'
print(str)      # its prints the whole string.

print(str[2:5]) #  its print the given index no.( only exclude the last index no.) 

print(str[2:])   # its prints all the string from given index no.

print(str * 4)   # its  print string 4 times.

print(str+ "Good morning") # its concatinate the string.
